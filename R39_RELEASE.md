# Byznio R39

- Dashboard CSS is loaded on both `/` and `/dashboard`.
- Dashboard body state is shared by both dashboard routes.
- Removed the dashboard sibling-hiding CSS rule.
- Mobile hamburger is now a real button with direct open/close JavaScript, independent of checkbox sibling selectors.
- Drawer close is a real button.
- Nia is forced visible on the dashboard with a high z-index.
- Build marker: 2026.09.22-r39.

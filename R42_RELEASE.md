# Byznio R42

- Fixed Dashboard SVG icons: Dashboard module icons are now rendered directly in the Dashboard view instead of depending on the shared icon helper.
- Fixed mobile bottom navigation icons with explicit inline SVG.
- Added cache-busting dashboard CSS version r42.
- Kept Nia behavior from R41: robot remains visible, proactive text is temporary and visual-only, no automatic speech; click/open panel remains unchanged.
- PHP syntax checked for modified views.
